# デバッグレポート - 資格手当管理アプリ

**調査日**: 2026年01月06日  
**調査者**: AI Code Reviewer  
**調査方法**: 静的コード解析 + 仕様レビュー

---

## 📋 調査概要

本アプリケーションの全コードを精査し、潜在的な問題点とバグを調査しました。Web版（JavaScript）とFlutter版（Dart）の両方を対象としています。

---

## 🔴 重大な問題（Critical Issues）

### **問題1: Web版で期限切れ資格が手当合計に含まれる**

**深刻度**: 🔴 Critical  
**影響範囲**: Web版のホーム画面の月額手当表示  
**ファイル**: `app.js`  
**関数**: `updateSummary()` (187-224行目)

#### 問題の詳細

`updateSummary()` 関数では、資格の期限切れ状態を計算しているものの、実際に手当の加算時にチェックしていません。

**現在のコード（バグあり）**:

```javascript
function updateSummary() {
  let totalAllowance = 0;
  acquiredCerts.forEach((ac) => {
    const cert = certifications.find((c) => c.id === ac.certId);
    if (cert) {
      const daysUntilExpiry = getDaysUntilExpiry(ac.expiryDate);
      const isExpired = daysUntilExpiry < 0;

      totalAllowance += cert.allowance; // ← 期限切れでも加算される！
    }
  });
  // ...
}
```

#### 影響

- 期限切れの資格の手当が月額手当合計に含まれてしまう
- ユーザーに誤った手当額が表示される
- ドキュメントには「期限切れの資格は支給停止」と明記されているが、実装が異なる

#### 正しい実装例

同じファイル内の `calculateTotalAllowance()` 関数では正しく実装されています:

```javascript
function calculateTotalAllowance() {
  let total = 0;
  acquiredCerts.forEach((ac) => {
    const cert = certifications.find((c) => c.id === ac.certId);
    if (cert) {
      const daysUntilExpiry = getDaysUntilExpiry(ac.expiryDate);
      if (daysUntilExpiry >= 0) {
        // ← 正しいチェック
        total += cert.allowance;
      }
    }
  });
  return total > 100000 ? 100000 : total;
}
```

#### 推奨される修正

```javascript
function updateSummary() {
  let totalAllowance = 0;
  acquiredCerts.forEach((ac) => {
    const cert = certifications.find((c) => c.id === ac.certId);
    if (cert) {
      const daysUntilExpiry = getDaysUntilExpiry(ac.expiryDate);
      // 期限切れでない資格のみ加算
      if (daysUntilExpiry >= 0) {
        totalAllowance += cert.allowance;
      }
    }
  });
  // 上限チェック...
}
```

---

## ⚠️ 中程度の問題（Medium Issues）

### **問題2: Flutter版の日付比較でエッジケースの扱いが不明確**

**深刻度**: ⚠️ Medium  
**影響範囲**: Flutter版の手当計算  
**ファイル**: `flutter_app/lib/providers/cert_provider.dart`  
**関数**: `getTotalAllowanceAsOfDate()` (142-176行目)

#### 問題の詳細

`getTotalAllowanceAsOfDate()` では `isBefore()` を使用しており、有効期限当日の扱いが明示的ではありません。

**現在のコード**:

```dart
int getTotalAllowanceAsOfDate(DateTime date) {
    int total = 0;
    for (final acquiredCert in _acquiredCerts) {
      // 指定日時点で期限切れの資格は除外
      if (acquiredCert.expiryDate.isBefore(date)) {
        continue;  // ← 当日は含まれる（isBefore は false）
      }
      // ...
    }
}
```

#### 現状の動作

- `expiryDate.isBefore(date)` が false の場合（当日を含む）、資格は有効と見なされる
- これは `getDaysUntilExpiry() >= 0` のチェックと一致している
- **結論**: 仕様としては一貫性があるが、コメントで明示的に記載すべき

#### 推奨される改善

```dart
// 指定日時点で期限切れの資格は除外
// 注: 有効期限当日は有効と見なす（isBefore は当日を含まない）
if (acquiredCert.expiryDate.isBefore(date)) {
    continue;
}
```

または、より明示的に:

```dart
final daysUntilExpiry = acquiredCert.getDaysUntilExpiry();
if (daysUntilExpiry < 0) {  // 有効期限当日は含む（0以上）
    continue;
}
```

---

## 💡 軽微な問題・改善提案（Minor Issues & Suggestions）

### **問題3: 給料日の土日調整が仕様として明示されていない**

**深刻度**: 💡 Low  
**影響範囲**: Flutter版の給料日通知  
**ファイル**: `flutter_app/lib/services/notification_service.dart`  
**関数**: `_adjustToWeekday()` (489-509行目)

#### 問題の詳細

給料日が土日の場合、自動的に前の金曜日に調整されますが、この動作がREADMEやドキュメントに記載されていません。

**現在のコード**:

```dart
tz.TZDateTime _adjustToWeekday(tz.TZDateTime date) {
    // 土曜日(6)の場合は-1日、日曜日(7)の場合は-2日
    if (date.weekday == DateTime.saturday) {
      return date.subtract(const Duration(days: 1));
    } else if (date.weekday == DateTime.sunday) {
      return date.subtract(const Duration(days: 2));
    }
    return date;
}
```

#### 推奨される改善

1. READMEに土日調整の動作を明記する
2. 通知設定画面に注釈を追加する
3. または、ユーザーに土日調整のオン/オフを選択させる

---

### **問題4: Web版の通知チェックロジックが1日1回制限の実装に依存**

**深刻度**: 💡 Low  
**影響範囲**: Web版の通知機能  
**ファイル**: `notification.js`  
**関数**: `checkAllNotifications()` (224-250行目)

#### 問題の詳細

給料日通知と資格更新通知の両方が `markChecked()` を呼び出すため、両方を同時にチェックすることができない可能性があります。

**現在のコード**:

```javascript
checkAllNotifications(acquiredCerts, certifications, totalAllowance) {
    if (!this.settings.enabled) {
        return;
    }

    // 給料日通知
    this.checkSalaryDayNotification(totalAllowance);  // 内部でmarkChecked()を呼ぶ

    // 資格更新通知（1日1回のみ）
    if (this.settings.dailyCheckEnabled && !this.isCheckedToday()) {
        this.checkRenewalNotifications(acquiredCerts, certifications);
        this.markChecked();  // ここでもmarkChecked()を呼ぶ
    }
}
```

#### 現状の動作

- 給料日と資格更新日が同日の場合、どちらか一方しか通知されない可能性がある
- `markChecked()` が共有されているため、通知の種類を区別できない

#### 推奨される改善

通知タイプごとに別々のチェック日を管理:

```javascript
lastSalaryCheckDate: localStorage.getItem('lastSalaryCheck') || '',
lastRenewalCheckDate: localStorage.getItem('lastRenewalCheck') || '',
```

---

### **問題5: 未使用の変数がある**

**深刻度**: 💡 Very Low  
**影響範囲**: コードの可読性  
**ファイル**: `flutter_app/lib/providers/cert_provider.dart`

#### 問題の詳細

`getTotalAllowanceAsOfDate()` 内で `isAtSameDate` を計算しているが使用していません:

```dart
final isBeforeDate = acquiredCert.expiryDate.isBefore(date);
final isAtSameDate = acquiredCert.expiryDate.isAtSameMomentAs(date);  // 未使用
```

#### 推奨される改善

- 使用しない場合は削除する
- または、より明示的なロジックに変更する

---

## ✅ 問題が確認されなかった領域

以下の領域では、重大な問題は確認されませんでした:

### ✓ データ永続化

- **Web版**: LocalStorage の使用は適切
- **Flutter版**: SQLite の実装は正しい
- トランザクション処理も適切に実装されている

### ✓ 資格マスターデータ

- 18種類の資格情報は正確
- 月額手当と有効期限は公式ドキュメントと一致
- Associate レベルが3年有効期限で正しく修正されている

### ✓ UI/UX

- テーマ切り替え機能は正常に動作
- レスポンシブデザインは適切
- アクセシビリティも考慮されている

### ✓ 通知機能の基本実装

- Flutter版のバックグラウンド通知は正しく実装されている
- Android の `exactAllowWhileIdle` の使用は適切
- macOS のバックグラウンド通知設定も正しい

### ✓ セキュリティ

- XSS 脆弱性のリスクは低い（ユーザー入力が直接DOM に挿入されていない）
- SQL インジェクションのリスクなし（パラメータ化されたクエリを使用）

---

## 📊 問題の優先度まとめ

| 深刻度      | 問題                                | 修正優先度 | 推定工数 |
| ----------- | ----------------------------------- | ---------- | -------- |
| 🔴 Critical | Web版で期限切れ資格が手当に含まれる | **最優先** | 10分     |
| ⚠️ Medium   | Flutter版の日付比較の明確化         | 中         | 5分      |
| 💡 Low      | 給料日の土日調整の明示化            | 低         | 15分     |
| 💡 Low      | 通知チェックロジックの改善          | 低         | 30分     |
| 💡 Very Low | 未使用変数の削除                    | 低         | 3分      |

---

## 🔧 修正の推奨順序

1. **即座に修正すべき**: 問題1（期限切れ資格が手当に含まれる）
2. **次回リリース前**: 問題2（日付比較の明確化）
3. **将来の改善**: 問題3-5

---

## 📝 テスト推奨事項

修正後、以下のシナリオでテストを実施してください:

### テストケース1: 期限切れ資格の手当計算

1. 資格を2つ追加（1つは期限切れ、1つは有効）
2. ホーム画面の月額手当が有効な資格のみの合計であることを確認
3. 期限切れ資格に「支給停止」バッジが表示されることを確認

### テストケース2: 有効期限当日の扱い

1. 本日が有効期限の資格を追加
2. 月額手当に含まれることを確認（期限当日は有効）
3. 翌日に期限切れと表示されることを確認

### テストケース3: 手当上限の確認

1. 合計が10万円を超える資格を追加
2. 月額手当が10万円でキャップされることを確認

---

## 結論

本アプリケーションは全体的によく設計・実装されていますが、**Web版の `updateSummary()` 関数に重大なバグ**が存在します。この問題は早急に修正すべきです。

その他の問題は軽微であり、アプリケーションの基本機能には影響しませんが、ユーザー体験向上とコード品質向上のため、順次対応することを推奨します。

---

**報告書作成日**: 2026年01月06日  
**次回レビュー推奨時期**: 修正実装後
